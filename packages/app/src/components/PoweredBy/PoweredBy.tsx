export function PoweredBy() {
  return (
    <p
      style={{
        fontSize: '0.6875rem',
        color: 'var(--primary)',
        opacity: 0.35,
        textAlign: 'center',
        padding: '0.25rem 0',
        fontFamily: 'var(--font-family)',
      }}
    >
      Routing by GraphHopper · Map © OpenStreetMap
    </p>
  )
}
